# Use the final HTTPS API origin; redirects can change mutation request methods.
API_BASE_URL=https://api.example.com

# Public by design: web and Flutter clients must use the same value.
# This is a deterministic protocol key, not a server-side password-storage secret.
NEXT_PUBLIC_PASSWORD_HASH_SECRET_KEY=replace-with-the-agreed-cross-platform-key
