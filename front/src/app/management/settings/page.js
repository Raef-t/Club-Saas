import SettingsClient from "@/app/management/settings/SettingsClient";

export const metadata = {
  title: "الإعدادات | TechnoGYM",
};

export default function SettingsPage() {
  return <SettingsClient />;
}
