# توثيق تعديل آلية الشركاء وحساباتهم وحمايتهم أمنياً

تم إدخال حزمة تعديلات متكاملة على نظام المحاسبة في معهد العلماء لضمان:
1. **الأتمتة الكاملة:** توليد حسابات فرعية مستقلة للشركاء (رأس المال والمسحوبات) لمنع تداخل الحسابات.
2. **الأمان (Authorization):** حظر الوصول إلى مسارات موديول المحاسبة وحصرها على الأدوار المخولة فقط.
3. **سلامة العمليات والمنطق:** التحقق من أن مجموع حصص أرباح الشركاء النشطين لا يتجاوز 100%.
4. **تكامل لوحة التحكم:** توفير واجهات لتعديل الشركاء، تعطيلهم (تجميدهم)، وحذفهم المشروط.

التالي هو توثيق شامل للتغييرات البرمجية لتطبيقها على أي نسخة أخرى من النظام.

---

## 1. مسارات الباك إند والحماية الأمنية (`routes/api.php`)

* **المسار:** `backend/Modules/Accounting/routes/api.php`
* **التعديلات:**
  1. أضفنا وسيط الأدوار `'role:super_admin|admin|accountant'` لـ `Route::group`.
  2. أضفنا مسار الحذف المخصص للشركاء.

```php
Route::group([
    'middleware' => ['api', 'auth:sanctum', 'approved', 'force-password-change', 'role:super_admin|admin|accountant'],
    'prefix'     => 'accounting',
    'as'         => 'api.accounting.',
], function () {
    // ...
    Route::put('partners/{id}', [PartnerController::class, 'update'])->name('partners.update');
    Route::delete('partners/{id}', [PartnerController::class, 'destroy'])->name('partners.destroy');
    // ...
});
```

---

## 2. قواعد التحقق والمنطق التجاري (Validation Requests)

### أ. عند إضافة شريك: `StorePartnerRequest.php`
* **المسار:** `backend/Modules/Accounting/Http/Requests/StorePartnerRequest.php`
* **التعديل:** جعل معرّفات الحسابات اختيارية، وإضافة تحقق مغلق (Closure) للتأكد من أن مجموع الحصص للشركاء النشطين مع الحصة الجديدة لا يتجاوز 100%.
* **كود التحقق المعدل:**
```php
public function rules(): array
{
    return [
        'name'                => 'required|string|max:100',
        'capital_account_id'  => 'nullable|integer|exists:acc_accounts,id',
        'drawings_account_id' => 'nullable|integer|exists:acc_accounts,id',
        'profit_share_pct'    => [
            'required',
            'numeric',
            'min:0',
            'max:100',
            function ($attribute, $value, $fail) {
                $currentSum = \Modules\Accounting\Models\AccPartner::where('is_active', true)->sum('profit_share_pct');
                if ($currentSum + $value > 100) {
                    $fail('مجموع نسب الأرباح للشركاء النشطين لا يمكن أن يتجاوز 100%. النسبة المتبقية المتاحة هي: ' . (100 - $currentSum) . '%');
                }
            }
        ],
        'joined_at'           => 'required|date',
        'is_active'           => 'nullable|boolean',
        'notes'               => 'nullable|string',
    ];
}
```

### ب. عند تعديل شريك: `UpdatePartnerRequest.php`
* **المسار:** `backend/Modules/Accounting/Http/Requests/UpdatePartnerRequest.php`
* **التعديل:** إضافة تحقق يمنع تعديل الحصة أو إعادة التنشيط إذا كان مجموع النسب للشركاء النشطين الآخرين مع القيمة الجديدة سيتجاوز 100%.
* **كود التحقق المعدل:**
```php
public function rules(): array
{
    return [
        'name'                => 'sometimes|string|max:100',
        'capital_account_id'  => 'sometimes|integer|exists:acc_accounts,id',
        'drawings_account_id' => 'nullable|integer|exists:acc_accounts,id',
        'profit_share_pct'    => [
            'sometimes',
            'numeric',
            'min:0',
            'max:100',
            function ($attribute, $value, $fail) {
                $partnerId = $this->route('id');
                $isActive = $this->input('is_active');
                if ($isActive === null) {
                    $partner = \Modules\Accounting\Models\AccPartner::find($partnerId);
                    $isActive = $partner ? $partner->is_active : true;
                }
                if ($isActive) {
                    $currentSum = \Modules\Accounting\Models\AccPartner::where('is_active', true)
                        ->where('id', '!=', $partnerId)
                        ->sum('profit_share_pct');
                    if ($currentSum + $value > 100) {
                        $fail('مجموع نسب الأرباح للشركاء النشطين لا يمكن أن يتجاوز 100%. النسبة المتبقية المتاحة هي: ' . (100 - $currentSum) . '%');
                    }
                }
            }
        ],
        'joined_at'           => 'sometimes|date',
        'is_active'           => [
            'nullable',
            'boolean',
            function ($attribute, $value, $fail) {
                if ($value === true || $value === 1 || $value === '1' || $value === 'true') {
                    $partnerId = $this->route('id');
                    $partner = \Modules\Accounting\Models\AccPartner::find($partnerId);
                    if ($partner) {
                        $share = $this->input('profit_share_pct', $partner->profit_share_pct);
                        $currentSum = \Modules\Accounting\Models\AccPartner::where('is_active', true)
                            ->where('id', '!=', $partnerId)
                            ->sum('profit_share_pct');
                        if ($currentSum + $share > 100) {
                            $fail('لا يمكن تفعيل الشريك لأن مجموع نسب الأرباح سيتجاوز 100%. النسبة المتبقية المتاحة هي: ' . (100 - $currentSum) . '%');
                        }
                    }
                }
            }
        ],
        'notes'               => 'nullable|string',
    ];
}
```

---

## 3. منطق الإضافة والحذف الآمن في المتحكم (`PartnerController.php`)

* **المسار:** `backend/Modules/Accounting/Http/Controllers/PartnerController.php`
* **التعديلات:**
  1. توليد الحسابات تلقائياً (رأس مال ومسحوبات) بأرقام تسلسلية تحت الآباء `3100` و `3300` عند إضافة شريك بدون تحديد الحسابات يدوياً.
  2. إضافة تابع `destroy` للتحقق من عدم وجود قيود مالية قبل السماح بالحذف، وإجراء الحذف الآمن (حذف الشريك أولاً ثم حساباته لضمان سلامة قاعدة البيانات).

* **الأكواد البرمجية للإضافة والحذف:**
```php
    public function store(StorePartnerRequest $request)
    {
        try {
            $data = $request->validated();

            $partner = DB::transaction(function () use ($data) {
                // 1. إنشاء حساب رأس المال تلقائياً إن لم يرسل
                if (empty($data['capital_account_id'])) {
                    $parentCapital = AccAccount::where('code', '3100')->first();
                    if (!$parentCapital) {
                        throw new \Exception('الحساب الرئيسي لرأس المال (3100) غير موجود في شجرة الحسابات.');
                    }

                    $maxCode = AccAccount::where(function($q) use ($parentCapital) {
                        $q->where('parent_id', $parentCapital->id)
                          ->orWhere('code', 'like', '31%');
                    })->where('code', '!=', '3100')->max('code');

                    $newCode = !$maxCode ? '3101' : (string) (intval($maxCode) + 1);

                    $capitalAccount = AccAccount::create([
                        'code'               => $newCode,
                        'name'               => 'رأس مال الشريك - ' . $data['name'],
                        'name_en'            => 'Capital - ' . $data['name'],
                        'type'               => 'equity',
                        'currency'           => 'BOTH',
                        'parent_id'          => $parentCapital->id,
                        'allow_manual_entry' => true,
                        'is_active'          => true,
                    ]);

                    $data['capital_account_id'] = $capitalAccount->id;
                }

                // 2. إنشاء حساب المسحوبات تلقائياً إن لم يرسل
                if (empty($data['drawings_account_id'])) {
                    $parentDrawings = AccAccount::where('code', '3300')->first();
                    if (!$parentDrawings) {
                        throw new \Exception('الحساب الرئيسي للمسحوبات الشخصية (3300) غير موجود في شجرة الحسابات.');
                    }

                    $maxCode = AccAccount::where(function($q) use ($parentDrawings) {
                        $q->where('parent_id', $parentDrawings->id)
                          ->orWhere('code', 'like', '33%');
                    })->where('code', '!=', '3300')->max('code');

                    $newCode = !$maxCode ? '3301' : (string) (intval($maxCode) + 1);

                    $drawingsAccount = AccAccount::create([
                        'code'               => $newCode,
                        'name'               => 'مسحوبات الشريك - ' . $data['name'],
                        'name_en'            => 'Drawings - ' . $data['name'],
                        'type'               => 'equity',
                        'currency'           => 'BOTH',
                        'parent_id'          => $parentDrawings->id,
                        'allow_manual_entry' => true,
                        'is_active'          => true,
                    ]);

                    $data['drawings_account_id'] = $drawingsAccount->id;
                }

                return AccPartner::create($data);
            });

            return $this->successResponse(
                new AccPartnerResource($partner->load('capitalAccount', 'drawingsAccount')),
                'تم إضافة الشريك بنجاح وتوليد حساباته تلقائياً',
                201
            );
        } catch (\Exception $e) {
            return $this->error($e->getMessage(), 500);
        }
    }

    public function destroy($id)
    {
        try {
            $partner = AccPartner::findOrFail($id);

            // التحقق من وجود قيود يومية لحساب رأس المال أو المسحوبات
            $capitalEntriesCount = DB::table('acc_journal_entries')
                ->where('account_id', $partner->capital_account_id)
                ->count();

            $drawingsEntriesCount = 0;
            if ($partner->drawings_account_id) {
                $drawingsEntriesCount = DB::table('acc_journal_entries')
                    ->where('account_id', $partner->drawings_account_id)
                    ->count();
            }

            if ($capitalEntriesCount > 0 || $drawingsEntriesCount > 0) {
                return $this->error('لا يمكن حذف الشريك لوجود حركات مالية مسجلة على حساباته. يمكنك تعطيله بدلاً من ذلك.', 400);
            }

            // الحذف الآمن للشريك وحساباته
            DB::transaction(function () use ($partner) {
                $capitalAccountId = $partner->capital_account_id;
                $drawingsAccountId = $partner->drawings_account_id;

                $partner->delete();

                AccAccount::where('id', $capitalAccountId)->delete();
                if ($drawingsAccountId) {
                    AccAccount::where('id', $drawingsAccountId)->delete();
                }
            });

            return $this->successResponse(null, 'تم حذف الشريك وحساباته بنجاح');
        } catch (\Exception $e) {
            return $this->error($e->getMessage(), 500);
        }
    }
```

---

## 4. ربط وتطوير الواجهات (RTK Query & React Frontend)

### أ. ملف التخزين: `accountingApi.js`
* **المسار:** `web/dashboard/olamaa-dashboard/src/store/services/accountingApi.js`
* **التعديل:** إضافة وتصدير طفرات (Mutations) التعديل والحذف.
```javascript
    updatePartner: builder.mutation({
      query: ({ id, ...data }) => ({ url: `/accounting/partners/${id}`, method: "PUT", data }),
      invalidatesTags: ["AccPartners"],
    }),
    deletePartner: builder.mutation({
      query: (id) => ({ url: `/accounting/partners/${id}`, method: "DELETE" }),
      invalidatesTags: ["AccPartners"],
    }),
```
* **المستخرج التلقائي:** تصدير `useUpdatePartnerMutation` و `useDeletePartnerMutation`.

### ب. ملف الواجهة: `partners/page.js`
* **المسار:** `web/dashboard/olamaa-dashboard/src/app/(accounting)/partners/page.js`
* **التعديلات:**
  1. إخفاء حقول الحسابات عند الإضافة.
  2. إضافة أزرار للتعديل والحذف على كرت كل شريك.
  3. ربط وتطبيق نموذج تعديل الشريك (Edit Partner Modal) يدعم تعديل الاسم، الحصة، والملاحظات وحالة النشاط.
  4. التحقق والتحذير قبل إرسال طلب الحذف للباك إند.
